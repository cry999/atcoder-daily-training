# イベント処理系 精選10問

## 復習リスト

- ABC128 E - 1
- ABC174 F
- ABC309 F
    - 解けはしたけど、時間かかったので
- ABC253 F

## チェックリスト

- [x] ABC221 D - Online games
  開始・終了イベント

- [x] ABC188 D - Snuke Prime
  イベント間の寄与

- [x] ABC256 D - Union of Interval
  区間の和集合

- [x] ABC128 E - Roadwork
  有効要素の追加・削除

- [x] ABC351 F - Double Sum
  走査線＋BIT

- [x] ABC174 F - Range Set Query
  オフラインクエリ

- [x] ABC309 F - Box in Box
  同一座標の処理順

- [x] Typical90 017 - Crossing Segments
  交差判定＋BIT

- [x] ABC253 F - Operations on a Matrix
  先読み＋差分

- [ ] ABC327 F - Apples
  尺取り＋遅延SegTree

---

## 3. ABC256 D - Union of Interval

区間 `[L_i, R_i)` に対して、

* `L_i` で `+1`
* `R_i` で `-1`

とする。

被覆数が、

* `0 → 1`：区間開始
* `1 → 0`：区間終了

となる地点を記録する。

**要点:** 状態そのものではなく、状態の変化を見る。

---

## 4. ABC128 E - Roadwork

工事 `(S_i, T_i, X_i)` は、クエリ時刻上では

```text
[S_i - X_i, T_i - X_i)
```

の間だけ有効になる。

クエリを時刻順に処理し、有効な `X_i` の最小値を管理する。

**要点:** クエリ時点で有効な要素だけをデータ構造に残す。

---

## 5. ABC351 F - Double Sum

左から `A_j` を処理する。

過去の `A_i < A_j` について、寄与の合計は

```text
A_j × 個数 - 総和
```

で求められる。

BITを使って、過去要素の個数と総和を管理する。

**要点:** 過去の情報を個数と総和に集約する。

---

## 6. ABC174 F - Range Set Query

クエリを右端 `r` の昇順に並べる。

各色について、最後に登場した位置だけをBIT上で `1` にする。

区間 `[l, r]` の和が、その区間に含まれる色の種類数になる。

**要点:** クエリを答えやすい順に並べ替える。

---

## 7. ABC309 F - Box in Box

箱を第一辺の昇順に処理する。

第二辺を添字とし、第三辺の最小値をSegment Treeで管理する。

同じ第一辺を持つ箱は、

1. 全て問い合わせる
2. その後で全て追加する

という順番で処理する。

**要点:** 狭義不等号では、同一座標の処理順が重要。

---

## 8. Typical90 017 - Crossing Segments

弦の交差条件を、端点の大小関係に変換する。

一方の端点順に処理し、もう一方の端点をBITで管理する。

**要点:** 幾何条件を一次元の順序問題に落とす。

---

## 9. ABC253 F - Operations on a Matrix

マスの値は、次の形で求める。

```text
直前の行代入値
+ 現在までの列加算
- 行代入時点までの列加算
```

値取得クエリを、直前の行代入イベントに事前に紐づける。

**要点:** 基準時点を保存し、現在との差分を取る。

---

## 10. ABC327 F - Apples

時間順に走査し、

* 時間窓に入ったりんごを追加
* 時間窓から出たりんごを削除

する。

位置方向では区間加算を行い、全体の最大値を遅延Segment Treeで求める。

**要点:** 一方の軸を走査し、もう一方の軸をデータ構造で管理する。

---

## おすすめ順

### 基礎

* [ ] ABC221 D
* [ ] ABC188 D
* [ ] ABC256 D

### データ構造併用

* [ ] ABC128 E
* [ ] ABC351 F
* [ ] ABC174 F
* [ ] ABC309 F

### 発展

* [ ] Typical90 017
* [ ] ABC253 F
* [ ] ABC327 F

---

## ABC467 E 復習チェック

* [ ] 何をイベントとして列挙するか説明できる
* [ ] イベント間の変化が単純になる理由を説明できる
* [ ] イベントをソートする理由を説明できる
* [ ] 差分更新の式を再現できる
* [ ] 解説を見ずに再実装できる
